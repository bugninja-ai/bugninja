[task]
name = "6_extra_instructions_navigation"
start_url = "https://amazon.com"  # REQUIRED: URL where the browser session should start
description = "Search for \"wireless headphones\", browse through product listings"
extra_instructions = [
    "Open exactly three different(!) product pages after each other",
    "On the product’s page read its average score",
    "Compare scores across all three products",
    "Navigate back to previously viewed products if necessary",
    "Add the product with the best single average score to the cart",
    "Close the browser afterwards",
]
allowed_domains = []
dependencies = []  # Optional: List of task folder names or CUIDs that this task depends on

[run_config]
# CLI-specific runtime configuration
viewport_width = 1920
viewport_height = 1080
user_agent = ""
wait_between_actions = 1.0
enable_vision = true
enable_healing = true
headless = false
enable_video_recording = true

[metadata]
task_id = "bpqrwe5s1hkswjbalcd8zw4k"
created_date = "2025-10-09T17:19:15.850365+00:00Z"
creation_type = "manually_added"
